image: $GITLAB_CI_IMAGE

default:
  interruptible: true

stages:
  - check_permission
  - publish
  - announce

include:
  - component: $CI_SERVER_FQDN/${SNAPSHOT_COMPONENT}

    inputs:
      enable_announce: true
