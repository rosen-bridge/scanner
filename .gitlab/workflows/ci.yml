image: $GITLAB_CI_IMAGE

default:
  interruptible: true

workflow:
  auto_cancel:
    on_job_failure: all

stages:
  - ci

include:
  - component: $CI_SERVER_FQDN/${CI_COMPONENT}

    inputs:
      enable_knip: true
      enable_lint: true
      enable_test: true
      enable_changeset: true
